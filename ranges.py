for i in range(1, 21, 5): # It will default from zero if there is no start value.
                          # We can provide a step value as well using a 3rd comma place
                          # Using a -2 begins the range from behind
    print("i is now {}".format(i))
